{% extends '::layout.html.twig' %}

{% block content %}

    {% set runtimeConfig = {"thumbnail": {"size": [width, height] }} %}

    {%- set imagePath -%}
        {% path image, 'reference' %}
    {%- endset -%}

{% dump imagePath %}
    <img src="{{ imagePath | imagine_filter('q90', runtimeConfig) }}"/>

{% endblock %}
